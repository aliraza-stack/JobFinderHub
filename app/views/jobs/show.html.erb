<div class="bg-gray py-3 mb-4 ">
  <h2 class="ms-5 mt-3 fw-bold">Job Detail Board</h2>
</div>

<% if current_user.role == "user" %>
  <div class="container d-flex flex-row gap-3 justify-content-end">
    <%= link_to "Edit this job", edit_job_path(@job), class:"btn btn-primary" %>
    <%= button_to "Destroy this job", @job, method: :delete, class:"btn btn-danger" %>
  </div>
  <% end %>


<%= render @job %>
